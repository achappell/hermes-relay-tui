# Friction Log

This is a capture queue for problems noticed during use. An entry does not
automatically become the next task.

## Triage rules

- Log the symptom, impact, and a possible next action while it is fresh.
- Fix immediately only when it blocks the current task, risks data loss, or
  is small enough to remove safely in a few minutes.
- Otherwise leave it as `deferred` and keep working.
- Promote an item when it becomes blocking, repeats, or naturally matches the
  next parity slice.
- Mark it `resolved` with the change and verification when it is fixed.

## Entries

| Date | Area | Friction | Impact | Next action | Status |
| --- | --- | --- | --- | --- | --- |
| 2026-08-28 | Composer | Enter submissions waited behind the active response, making the TUI appear unable to accept the next prompt. | High: blocked the queue workflow. | Run submissions in a background worker while preserving one WebSocket reader. | resolved |
| 2026-08-28 | Voice protocol | The current voice-session channel has no explicit interrupt operation, so a client-side interrupt cannot guarantee that Hermes stops server-side generation. | Medium: interruption is safe locally but remote cancellation is best-effort. | Add and wire a server-side `interrupt` operation to the voice-session protocol. | deferred |
| 2026-08-28 | Voice UX | Audio state is now separated from the response header, but the transcript still carries too much lifecycle detail for a calm, polished voice experience. | Low: functional output is clear, but status presentation can feel noisy. | Give voice lifecycle state a dedicated compact status surface and refine speaking/buffering transitions. | deferred |
| 2026-08-28 | Transcript rendering | Thinking/status updates repeat as separate transcript lines, and the final response begins on one of those activity lines instead of a clean assistant-message boundary. | High: long turns become noisy and the answer is difficult to scan. | Render thinking/tool activity in a replaceable lane, suppress duplicate updates, and start the final response as its own polished message block. | resolved: normalized activity rendering and response boundaries; covered by app tests |
| 2026-08-28 | Interaction settings | Steering was modeled as its own slash command, but it should be a setting that changes what an ordinary message does while a turn is active. | Medium: the old command shape did not match Hermes TUI behavior. | Add `queue`, `steer`, and `interrupt` busy modes; make ordinary submissions follow the selected mode; expose `/busy` for session changes; remove the legacy slash command. | resolved |
| 2026-08-28 | Command completion | Current `Tab` completion edits the composer inline; it lacked the searchable visual command overlay used by Claude TUI. | Medium: commands were discoverable only if the user already knew the slash syntax. | Added `/`-triggered palette with live filtering, descriptions, argument hints, arrow-key selection, and Escape-to-close behavior; verified in the app suite and full suite. | resolved |
| 2026-08-29 | Command completion | Starting a slash command opens a blocking selection flow; after choosing a command, the user must type its arguments and submit separately. | Medium: command entry feels interrupted and slower than ordinary prompt entry. | Keep inline command suggestions visible while typing; let `Tab` accept a completion without taking focus away, preserve editable arguments, and let `Enter` submit directly without requiring an overlay selection. | deferred |
| 2026-08-28 | Homebrew packaging | Homebrew rejects a formula file supplied directly from a project checkout; formulas must be staged inside a tap. | Medium: the Jensen trial could not use a single `brew install` command until a tap existed. | resolved: published the public tap and pinned its Git source to `v0.1.0`; release automation can open formula update PRs after its tap token is configured. |
| 2026-08-28 | Public repository privacy | The public README and defaults exposed a private network endpoint. | High: the endpoint did not belong in a public repository. | Use a localhost default and require an explicit URL or environment variable for remote gateways; historical commits still need a deliberate cleanup decision. | resolved |
| 2026-08-29 | Audio fallback | A speaker can fail after live playback starts while buffered PCM remains available, but the client treated the turn as successfully played and discarded the fallback audio. | Medium: a response could be lost when the output device drops mid-turn. | Track playback failure and save buffered PCM as a recovery WAV. | resolved: added playback-failure tracking and a regression test. |
| 2026-08-29 | Duplicate final text | A live voice turn rendered the same response twice when a whole-text frame was followed by an identical `message.complete` frame. | High: the answer looked duplicated even though voice transport succeeded. | Remember whole-text frames in the client’s deduplication state. | resolved: added normalization state tracking and a regression test; live rerun confirmed one response. |
| 2026-08-29 | Duplicate final text — raw delta path | The real transcript smoke test duplicated the answer when Hermes sent multiple raw `message.delta` chunks followed by the full `message.complete` text. | High: the transcript answer was visibly repeated even though the turn completed. | Treat raw delta payloads as append-only; only diff an explicitly cumulative rendered preview. | resolved: fixed normalization, added regression coverage, and Amanda’s media-server smoke test now shows one response. |
| 2026-08-29 | Queue UX | Queued prompts are currently represented as transcript/system text, so the user has to infer what is waiting and what has already dequeued. | Medium: queueing works, but pending messages are not visually anchored to the composer or easy to watch as they move through the turn lifecycle. | Fold into UX-01: add a compact queue shelf rising from the text-entry box; show pending messages/count, then remove each item as it dequeues and begins sending. | deferred |
| 2026-08-30 | Composer keybinding | Shift+Enter does not insert a newline. Confirmed with `scripts/key_event_diagnostic.py` in Ghostty: Shift+Enter arrives as `ctrl+j` (bare linefeed), not a `shift+enter`-named key, so `Composer._on_key`'s `event.key.endswith("+enter")` check never matched it. | Medium: Alt+Enter still worked as a fallback, but the composer's advertised primary newline key was silently dead. | resolved: added an explicit `ctrl+j` case alongside the `+enter` check in `Composer._on_key` (app.py); regression test `test_ctrl_j_inserts_newline_without_submitting` added to `tests/test_app.py`. | resolved |
| 2026-08-30 | Thinking activity rendering | The literal `[thinking]` activity line is a placeholder, not a finished surface — it gives no sense of what's being reasoned about or how long it took. | Medium: functional (activity is visible and replaceable per TURN-01), but it reads as unfinished rather than polished. | Expand into an actual reasoning/thinking detail lane (not just a static label) plus a short elapsed-time summary once a turn's thinking completes, e.g. "thought for 4s". Likely folds into UX-01's polish pass alongside the queue shelf, since both are about giving transient turn state a real visual surface instead of a text placeholder. | deferred |
