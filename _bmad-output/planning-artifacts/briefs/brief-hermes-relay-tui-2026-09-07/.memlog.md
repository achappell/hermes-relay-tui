---
topic: Hermes Home Assistant Platform
updated: 2026-09-07T20:36
---

- (decision) Treat the product as one Hermes-agent-backed household assistant expressed through puck hardware, display hardware, and the TUI; this prevents separate front ends from becoming separate products.
- (decision) Keep ESP32 devices as responsive endpoints and keep intelligence/session orchestration in the Hermes relay/core; the microcontrollers are not model hosts.
- (event) Discovery grounded in HOME-02 wake, HOME-03 display, appliance-loop validation, shared display contract, ESP32-S3 firmware notes, and board item UI-CORE-05.
- (decision) Add the separate Hermes Relay iOS/macOS SwiftUI repository as a first-class companion surface for secure relay profiles, push-to-talk, local conversation continuity, streamed audio, and recovery; keep it independently buildable and do not force appliance or TUI UI parity.
- (assumption) Treat the household assistant as one product with four surface families—puck, display, Apple client, and TUI—while shared protocol/domain semantics remain the parity target and presentation/capability parity remains intentionally selective.
- (decision) Use the Coaching path: develop the brief section by section through concrete household scenarios, challenging thin assumptions before polishing the document.
- (decision) Use the dinner-in-the-kitchen interaction as the north-star scenario: Amanda says Hey Missy, the device reaches Hermes profile media-server amanda, the agent reads authorized vault dinner plans, and the answer returns without a phone or terminal.
- (assumption) The assistant must distinguish authorized household context from generic knowledge and make profile/context selection part of the trusted experience, not an invisible implementation detail.
- (decision) Clarify product boundary: Missy is Amanda’s existing Hermes voice agent, currently reached through Photon text; Hermes supplies context and decides the response, while puck, display, iOS, TUI, and Photon are transport/presentation doorways and must not implement a second assistant persona.
- (change) Reframed the dinner scenario and differentiator from building an assistant persona to carrying the existing Missy/Hermes relationship into ambient hardware and companion surfaces.
- (decision) Each household device uses its own Hermes session. The `media-server amanda` agent profile and authorized vault context carry across surfaces, but Photon’s live conversation thread is not reused or merged into the device session.
- (open-question) Determine session lifetime across reconnects and reboot, and what local history should survive after a device-owned Hermes session ends.
- (assumption) For the first hardware milestone, assume a new Hermes session after reconnect; daily rotation remains undecided and should be configurable rather than embedded in firmware.
- (open-question) Session continuity is not required for the dinner north-star scenario; revisit whether daily rotation or another lifecycle policy improves follow-up behavior after the core hardware loop is proven.
- (decision) Shared devices support multiple household identities: distinct wake words route to distinct Hermes profiles and their authorized vault contexts before a turn is captured or submitted.
- (risk) Profile routing is a privacy boundary; unknown or ambiguous wake phrases and profile-mapping updates need explicit behavior so one person’s context cannot leak into another session.
- (decision) Room arbitration rule: when multiple devices hear a wake phrase, only the closest device responds, captures, and answers; all other devices remain silent and create no duplicate turn.
- (open-question) Determine the proximity signal and tie behavior for closest-device arbitration; keep the product requirement independent of the eventual sensor or audio-processing implementation.
- (decision) Nearby displays mirror the closest device’s active Hermes turn visually. The closest device owns wake, capture, audio, and the session; the display does not speak, capture, or create a duplicate session.
- (insight) The display is an ambient companion surface, not merely a second puck: it adds glanceable context to voice interactions while preserving one room-level turn.
- (decision) At rest, the display should show ambient photos/media. When a relevant calendar event requires leaving soon, it should replace the ambient surface with the event and a clear countdown or time remaining to leave.
- (open-question) Define soon, qualifying calendars/events, and the source and calculation for departure time, including travel and preparation time if needed.
- (decision) Only calendar events with a location and usable travel time can trigger the display’s departure countdown; home-based or locationless events do not displace ambient photos/media.
- (open-question) Set the soon threshold and fallback behavior when a qualifying event has no current routing data.
- (decision) The departure-display threshold is configurable rather than hardcoded; default timing remains open until household usage establishes a sensible baseline.
- (open-question) Decide where the household configures the departure threshold and whether it is global, per profile, per room, or per display.
- (decision) The configurable departure threshold belongs to the Hermes profile, matching each profile’s identity, calendar relevance, and routine.
- (open-question) Confirm each profile’s calendar source and departure origin so travel time is calculated against the right person and routine.
- (change) Revised calendar model: use a shared family calendar rather than profile-specific calendars; evaluate departure relevance per event, independent of which Hermes profile last spoke.
- (open-question) Decide whether each family-calendar event carries its own departure alert offset or inherits a shared family default; retain the event-level location and travel-time requirement.
- (decision) Compute family-calendar departure time from each event’s start time, usable route duration, and a configurable family preparation buffer; do not require an explicit leave-by field.
- (open-question) Define the route origin for departure calculations—presumably the household home—and the fallback when current routing data is unavailable.
- (decision) Use the household home as the fixed route origin for every qualifying family-calendar event; do not vary departure calculations by active Hermes profile or current device location.
- (open-question) Choose the display fallback when routing data is unavailable for a located family-calendar event: show the event without a countdown, use a configured estimate, or leave ambient media in place.
- (decision) When live routing is unavailable, calculate the departure card with a configured travel estimate expected from the vault, and label the result as estimated rather than presenting it as live traffic truth.
- (assumption) Assume Hermes or the vault provides configured travel estimates keyed to destinations; verify the actual source, shape, and freshness policy before implementation.
- (decision) Qualifying family-calendar departure alerts are household-wide: every display shows the same event and time-to-leave state, regardless of which room handled the last voice turn.
- (open-question) Define synchronization and missed-update recovery for the household-wide calendar alert across all displays.
- (decision) Use Immich as the source for ambient display photos; calendar departure alerts temporarily replace the photo surface on every display.
- (open-question) Choose the Immich library or album scope for ambient display and define how private or sensitive photos are excluded.
- (decision) Select ambient Immich photos by configured face/person filters; do not treat the entire Immich library as eligible by default.
- (risk) Face-based ambient selection needs explicit inclusion/exclusion and configuration rules so private or contextually inappropriate photos are not surfaced around the home.
- (decision) Ambient Immich face/person filters are configurable per room/display, allowing each screen to show the household members or visual context appropriate to that room.
- (open-question) Define the room/display behavior when its configured Immich face filters return no matching photos.
- (decision) When a room’s configured Immich face filters produce no matching photos, show a neutral ambient background; do not widen the filter or interrupt the household with setup UI.
- (decision) Version one is a voice doorway into Hermes, not a native smart-home platform. Clients do not duplicate integrations for timers, media, reminders, or devices; Hermes may perform actions exposed by the selected profile’s tools.
- (scope) Keep first-release client scope focused on wake, capture, routing, presentation, playback, display context, and recovery around Hermes turns.
- (decision) Version-one Hermes approval and clarification prompts are voice-only. The display may mirror prompt state/text, but no touch action submits or resumes a prompt response.
- (scope) Exclude touch-based prompt choice UI from the first release; preserve the touch display as an ambient and visual companion surface for later interaction work.
- (decision) Use the ReSpeaker Lite as the first puck prototype for the ESP32 voice endpoint; retain the product boundary of local wake/capture/playback plus Hermes routing.
- (assumption) Do not assume the exact ReSpeaker Lite variant or USB/I2S firmware yet; verify the attached ESP32 host, audio path, and XMOS front-end integration before hardware acceptance criteria.
- (decision) The first puck audio prototype combines ReSpeaker Lite for the voice front end with salvaged Google Home speaker hardware for output, subject to electrical and enclosure validation.
- (risk) The salvaged Google Home speaker may require its original amplifier or a compatible replacement; do not assume the ReSpeaker Lite can drive the raw speaker directly.
- (decision) The puck will transplant the salvaged Google Home speaker driver into a custom 3D-printed enclosure around the ReSpeaker Lite prototype.
- (risk) Validate the salvaged driver’s impedance/power requirements, compatible amplifier, enclosure acoustics, and thermal/safety behavior before treating the printed puck as a hardware acceptance target.
- (decision) For the first puck prototype, prefer an available 1.28-inch TFT as the local status surface; LEDs remain optional and are not required to prove the interaction loop.
- (open-question) Define the small TFT’s information boundary: local wake/turn status only, short response text, or a minimal mirror while the larger display carries visual answers.
- (decision) The puck’s 1.28-inch TFT is a glanceable wake/turn-status surface only; it does not render full response text or become a miniature transcript.
- (scope) Keep detailed response presentation on audio, the larger display mirror, iOS, and TUI; keep puck visual feedback compact and phase-oriented.
- (decision) Target visible wake acknowledgement and puck status within roughly one second of a recognized wake phrase; exact measurement can be tuned during hardware validation.
- (metric) Working responsiveness target: wake phrase to visible acknowledgement/status <= approximately 1 second.
- (decision) Accept approximately four seconds from end of speech to first spoken Hermes audio for version one; prioritize reliable routing, state honesty, and household context over latency optimization.
- (metric) Working first-version response target: end of speech to first spoken audio <= approximately 4 seconds.
- (decision) A false wake may briefly show listening status, but it must remain silent, submit no Hermes turn, and clear automatically without alarming the household.
- (acceptance) False-trigger validation: detection followed by silence produces no speech and no Hermes turn, then returns to ambient/idle state.
- (decision) Use an eight-second default listening window after wake for version one; keep it configurable so real household use can tune the pause without changing firmware behavior.
- (metric) False-wake recovery target: no speech or Hermes turn, return to ambient/idle within the configurable listening window; default 8 seconds.
- (decision) After each completed response, the active device stays in a bounded follow-up listening mode without another wake phrase; use the existing 8-second implementation default as a provisional value.
- (open-question) Confirm the follow-up listening duration and whether an explicit stop utterance exits the window silently.
- (decision) The exact utterance stop exits active capture or follow-up silently; it is not sent to Hermes and does not produce a replacement turn.
- (scope) Keep silent stop local to capture/follow-up; treat interrupting Missy during active playback as a separate barge-in decision with its own audio and echo-safety requirements.
- (decision) Defer barge-in during active Missy playback until the puck/display audio route has verified echo cancellation or equivalent isolation; keep version one focused on wake, capture, follow-up, and safe stop.
- (scope) Version-one stop applies to local capture/follow-up only; it does not interrupt an answer already being spoken.
- (decision) Do not make the TUI the household setup surface. Treat Hermes configuration/vault as the authority for agent and household context, with the iOS app as the likely device-facing setup/control surface.
- (open-question) Define the exact ownership split between Hermes config/vault and iOS for wake words, profile routing, room assignment, Immich face filters, family calendar, thresholds, and device credentials.
- (decision) When Hermes is unavailable, keep ambient Immich photos and family-calendar departure alerts running on displays; disable or honestly mark voice interaction without replacing useful context with a full-screen error.
- (acceptance) Offline degradation validation: stop Hermes, confirm displays retain photos/calendar alerts, wake does not send a turn, and voice unavailability is visible but non-disruptive.
- (decision) Show Hermes unavailability as a discreet persistent indicator on every display while retaining ambient photos and calendar context; do not replace the screen with a full-page error.
- (acceptance) Disconnected-display validation: indicator persists during Hermes outage, ambient surfaces remain usable, and the indicator clears after verified recovery.
- (decision) Devices reconnect to Hermes automatically after an outage and clear the persistent unavailable indicator after verified recovery; recovery must not silently reopen microphone capture or submit a turn.
- (acceptance) Recovery validation: restore Hermes after an outage, confirm automatic reconnection and indicator clearing, then require a fresh wake phrase before the next turn.
- (decision) The iOS app is a first-class full doorway into Missy for typed text and voice, with its own local conversation/history and honest connection state; it is not merely a setup console.
- (scope) First-release surface roles: puck/display provide ambient household voice; iOS provides portable full voice/text conversation plus setup/history; TUI provides engineering diagnostics and recovery.
- (decision) Every doorway owns an independent Hermes session: Photon, iOS, puck, display-owned voice endpoints, and TUI share profile/context configuration but never merge live conversation threads.
- (architecture) Session topology is per client/device; agent identity and authorized household context are shared through configuration, while conversation state remains session-local.
- (decision) Keep the TUI as a full typed/voice doorway into Missy for power use and diagnostics; exclude it from household configuration even though it can support setup-adjacent recovery.
- (scope) All four surface families remain conversation-capable in their appropriate modality; configuration ownership stays outside the TUI.
- (decision) Retain local conversation history in iOS and TUI only. Puck and display hardware keep conversation content ephemeral to the active interaction and do not become transcript archives.
- (privacy) Conversation persistence is intentionally limited to the full conversation clients; household appliances show active state/answer context without retaining full history.
- (architecture) ESP32 endpoints handle wake detection and audio capture; the trusted media-server performs speech-to-text because of ESP32 limits, then submits transcript text to Hermes. Keep raw microphone audio within the household path unless Hermes explicitly requires otherwise.
- (open-question) Define the media-server audio transport, buffering, retention, and failure behavior for captured speech before transcription.
- (decision) The first puck audio/transcription path is home-LAN-only: the ESP32 requires the local media-server and does not send raw audio through remote access.
- (constraint) Keep first-version speech capture inside the trusted household network; remote audio access is out of scope unless a later requirement earns it.
- (decision) Support both display surfaces in the first release: the ESP32-S3 touchscreen appliance and a browser/WASM kiosk running on iPad. Both consume the same room configuration, ambient Immich feed, family-calendar alerts, mirrored Hermes state, and recovery semantics.
- (scope) Treat the native macOS display simulator as development/validation tooling, not a third household product surface.
- (decision) Deploy the first iPad display through Safari/Guided Access pointed at the local media-server kiosk URL; do not build a packaged native iPad display app for the first release.
- (constraint) Keep the iPad display on the home LAN and reuse the existing web kiosk/state-channel path rather than adding a second native display runtime.
- (decision) Use LAN discovery as the first-release iOS device-enrollment path because the system is home-LAN-local; retain QR pairing as a fallback for networks or installations where discovery is unavailable.
- (rationale) LAN discovery avoids a pairing ceremony on the tiny puck and matches the local media-server architecture; QR/manual fallback can be added without changing the device/session model.
- (security) A newly discovered device remains unconfigured and inert until explicit iOS approval and room/profile assignment; no auto-enrollment or household-default identity.
- (acceptance) Enrollment validation: discover a new device, confirm it cannot wake/capture/access Hermes before approval, then approve and assign room/profile before it becomes active.
- (security) Removing a device in iOS immediately revokes its profile access and disables wake/capture, even if the physical device remains powered and reachable on the LAN.
- (acceptance) Revocation validation: remove an approved device in iOS, confirm access is rejected immediately, and verify the device cannot resume operation without re-approval.
- (security) Each approved puck/display receives its own revocable device credential; no hardware shares a profile bearer credential, so one device can be removed without invalidating the household’s other doorways.
- (open-question) Define per-device credential storage, rotation, expiry, and re-enrollment across ESP32, iPad kiosk, and other display targets.
- (decision) Keep calendar departure alerts quiet in version one: qualifying events appear visually on displays, while the puck makes no unsolicited spoken announcement or alert sound.
- (scope) Defer audible departure announcements and earcons until a later release with explicit household controls.
- (decision) When multiple devices hear the same wake phrase, the closest device wins; if proximity is effectively tied, the media-server applies configured per-device priority. Losing devices stay silent and create no duplicate Hermes turns.
- (acceptance) Arbitration validation: make two devices hear one wake phrase, confirm only the selected winner acknowledges/captures/answers, and confirm an effective tie resolves by configured priority without duplicate turns.
- (decision) For version one, every endpoint reconnect starts a fresh Hermes session. Do not impose a daily session rollover yet; revisit it only if household continuity needs justify the added policy.
- (acceptance) Session validation: after a dropped connection and successful reconnect, the endpoint receives a new Hermes session identity and does not resume or replay the prior live conversation.
- (decision) A shared puck or display may recognize multiple explicitly configured wake-word/profile mappings. The recognized wake word selects the Hermes profile before capture is submitted, and profile context remains isolated.
- (acceptance) Profile-routing validation: configure two wake-word/profile mappings on one device, invoke each phrase, and confirm the correct profile/session is selected before transcript submission with no cross-profile context.
- (security) If a recognized wake-word mapping points to an unavailable or revoked Hermes profile, the device fails closed: it captures no audio, submits nothing to Hermes, and shows only a local status indication.
- (acceptance) Fail-closed validation: revoke or make a mapped profile unavailable, invoke its wake word, and confirm no capture payload or Hermes turn is produced while the device reports a local unavailable state.
- (security) Wake-word mappings are unique within the household configuration: one phrase routes to one Hermes profile, and duplicate or ambiguous mappings are rejected before deployment.
- (acceptance) Mapping validation: attempt to configure one wake phrase for two profiles and confirm configuration is rejected without publishing either ambiguous mapping to a device.
- (decision) The iOS app is the sole authority for physical-device approval, room assignment, wake-word/profile mappings, per-device credentials, revocation, and re-enrollment. Hermes remains authoritative for agent, profile, and vault context.
- (security) Device administration has one control plane in version one: iOS; physical endpoints cannot self-approve, self-reassign, or restore access after revocation.
- (decision) Configuration ownership is split for v1: iOS owns room-level presentation policy, including Immich face filters and departure-alert thresholds; Hermes/vault owns the shared family calendar, household home, and configured travel estimates.
- (rationale) Keep household facts canonical in Hermes/vault and device-facing presentation choices in iOS so room preferences do not fork calendar or travel truth.
- (privacy) Raw puck audio is transient by default: the media-server may buffer it for capture and transcription, then discards it after successful handoff. Diagnostic recording is explicit opt-in and is never the default.
- (acceptance) Audio-retention validation: complete, failed, cancelled, and disconnected captures leave no raw-audio artifact by default; an explicit diagnostic opt-in is required before any recording is retained.
- (privacy) The media-server does not retain a transcript archive. Hermes owns live session handling; iOS and TUI may retain deliberate local histories, while puck and display surfaces remain transient.
- (acceptance) Transcript-retention validation: inspect the media-server after successful and failed turns and confirm no transcript archive is created; verify local history remains limited to the iOS/TUI clients.
- (privacy) Active Hermes response text and turn context are room-local: only the display in the winning device’s room mirrors the conversation. Household-wide calendar departure alerts are the deliberate shared-display exception.
- (acceptance) Display privacy validation: start a voice turn in one room and confirm only that room’s display receives response text/turn context, while all displays still receive a qualifying shared departure alert.
- (scope) The first shippable release is a complete single-room household pilot: one puck, its room-local display, the iPad browser kiosk, the iOS app, and the TUI. Contracts and configuration remain multi-room-ready, but a multi-room hardware rollout is not a v1 prerequisite.
- (acceptance) Pilot validation: complete the dinner-in-kitchen journey end to end through wake, profile routing, media-server transcription, Hermes response, room-local display mirroring, recovery, and iOS/TUI access, without requiring a second physical room.
- (decision) When the media-server or Hermes path is unavailable, the puck fails quiet: it does not capture speech or provide a synthetic spoken fallback and shows only its local unavailable state.
- (acceptance) Offline voice validation: disable the media-server or Hermes path, invoke a configured wake word, and confirm no microphone payload or spoken fallback is produced while the puck reports unavailable locally; recovery requires a fresh wake after verified reconnection.
- (decision) Missy is one Hermes voice profile, not the household platform’s product identity. The platform must support other profiles and keep its own product name/wake-word identity separate.
- (open-question) Choose the household platform product name and default wake phrase independently from Hermes profile names such as Missy.
- (decision) A calendar event receives a leave-time alert only when it has a usable live route or configured travel estimate. If neither exists, keep the event as ordinary calendar context and suppress the departure alert.
- (acceptance) Calendar validation: an event with no usable live route and no configured travel estimate never produces a time-to-leave alert, while the event remains available as normal calendar context.
- (decision) Departure-alert timing uses a household default threshold with optional per-event overrides. The event override wins when present; otherwise the shared household default applies.
- (acceptance) Threshold validation: an event without an override uses the household default, while an event with an override alerts at its own threshold without changing other events.
- (decision) Once a family event crosses its effective departure-alert threshold, the card replaces ambient photos on all displays until the event starts, then clears automatically back to the ambient surface.
- (acceptance) Departure-card validation: cross the effective threshold, confirm all displays promote the card and keep it visible, then advance past event start and confirm every display returns to ambient photos without manual dismissal.
- (decision) Shared-calendar updates immediately supersede an active departure card: cancellation clears it, while a changed time or location recomputes the route, threshold, and leave time across every display.
- (acceptance) Calendar-update validation: cancel an active event and confirm all cards clear; move its time or location and confirm every display recomputes or suppresses the card from the updated data.
- (decision) Keep “Hermes Home Assistant Platform” as the working title through the first-room pilot. The eventual product name remains separate from Hermes and its voice profiles; Missy is one profile, not the product name.
- (scope) Defer product naming until the first-room pilot has demonstrated the household experience and its identity.
- (change) Finalized the product brief after structure/prose review: condensed the brief to its product boundary, moved detailed decisions and validation into addendum.md, and promoted status to final.
