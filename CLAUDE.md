# CLAUDE.md

Read `AGENTS.md` in this repo before doing anything else — especially before
answering "what's next" or starting any work. Its "GitHub Project task
management — check this first" section is the operational source of truth
for task scope and priority: the GitHub Project (project #3, owner
achappell), not `docs/plans/`, not `docs/friction-log.md`, not this file.

`docs/friction-log.md` is a capture queue for problems noticed during use —
it feeds *into* GitHub project items when something becomes worth promoting,
but is never itself a source of tasks to pull from directly.
